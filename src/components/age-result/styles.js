import styled from 'styled-components';

const StyledNumber = styled.p`
	font-family: 'Poppins';
	font-style: italic;
	font-weight: 800;
	font-size: 5.875rem;
	color: #854dff;
	margin: 0 5rem;
`;

const StyledData = styled.span`
	color: black;
`;

export { StyledNumber, StyledData };
